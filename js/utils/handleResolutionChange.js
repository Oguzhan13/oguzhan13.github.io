// export function handleResolutionChange(resolutionThreshold, elementIdsToHide) {
//     function updateElementVisibility() {
//         const shouldHide = window.innerWidth <= resolutionThreshold;
        
//         for (const elementId of elementIdsToHide) {
//             const element = document.getElementById(elementId);
//             if (element) {
//                 element.style.display = shouldHide ? 'none' : 'block';
//             }
//         }
//     }

//     // Sayfa yüklendiğinde ve pencere boyutu değiştiğinde elementleri güncelle
//     window.addEventListener('DOMContentLoaded', updateElementVisibility);
//     window.addEventListener('resize', updateElementVisibility);
// }
